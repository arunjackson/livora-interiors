# Livora Interiors website

Open this folder in VS Code, then use the **Live Server** extension or run `python3 -m http.server 5500` in the terminal. Visit http://localhost:5500.

This is a visual front-end site. The inquiry form provides a demo confirmation only; connecting it to storage, email notifications, authentication, and an admin dashboard requires a backend service such as Supabase or Firebase.
